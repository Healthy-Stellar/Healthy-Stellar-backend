import { PharmacyReorderAlertProcessor } from './pharmacy-reorder-alert.processor';

describe('PharmacyReorderAlertProcessor (smoke)', () => {
    it('should instantiate', () => {
        expect(PharmacyReorderAlertProcessor).toBeTruthy();
    });
});

